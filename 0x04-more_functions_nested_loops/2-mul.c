#include "main.h"

/**
 * mul - this function multiplies 2 given arguments
 * @a: holds the first argument
 * @b: holds tge second argument
 * Return: returns the result of the a*b
 */

int mul(int a, int b)
{
int mult;
mult = a * b;
return (mult);
}
