#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a given string
 * @s: stores the pointer of the string passed to the function
 * Return: the length of string
 */

int _strlen(char *s)
{
int len = strlen(s);
return (len);
}
