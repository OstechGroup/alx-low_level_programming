#include <unistd.h>
#include <stdlib.h>
#include "main.h"

/**
 *_abs - returns the abs value of an argument
 *@ab: carries argument
 *Return: value of a
 */

int _abs(int ab)
{
int a = abs(ab);
return (a);
}
